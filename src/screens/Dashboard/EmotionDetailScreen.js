import ScreenWrapper from '../../layouts/ScreenWrapper';
import EmptyState from '../../components/EmptyState';

// Per-emotion score, explanation, and guidance; built in Phase 4.
const EmotionDetailScreen = () => (
  <ScreenWrapper>
    <EmptyState icon="🔍" title="Emotion Detail" description="Emotion detail views are coming in Phase 4." />
  </ScreenWrapper>
);

export default EmotionDetailScreen;

import ScreenWrapper from '../../layouts/ScreenWrapper';
import EmptyState from '../../components/EmptyState';

const HelpScreen = () => (
  <ScreenWrapper>
    <EmptyState icon="❓" title="Help" description="Help & support content is coming in Phase 5." />
  </ScreenWrapper>
);

export default HelpScreen;

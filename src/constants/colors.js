// Re-exported from styles/colors.js so there is a single source of truth
// for the palette while still satisfying the constants/ + styles/ structure.
const { COLORS } = require('../styles/colors');

module.exports = { COLORS };
